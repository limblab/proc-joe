function [distanceFromMain] = getDistanceData(heatmapData,mapFileName)
    %create array for distance of each stim chan from main
    distanceFromMain = [];
    
    %get row and col of main chain
    
    %calculate difference from main chan
    



end