function [output_data] = getEvokedSpikeLatencyData(array_data, input_data)
    
    % for each unit, determine if the unit is responsive at each amplitude
    % (simple 25% of stimuli evoke activity kind of thing)
    
    % find latency of evoked spikes in each volley if there are multiple 
    %   -- actual timing, mean and variance
    
    
    for i_unit = 1:numel(array_data)

        

    end








end