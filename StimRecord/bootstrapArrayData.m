%% resample each neuron in array data, calculate spikes in a window, store iterations
% and repeat

num_resamples = 10;

for arr_idx = 1:numel(arrayData)
    
    
    % build data string and store idx to identify which condition
    
    for n = 1:num_resamples
        % datasample data with replacement
    
        % use idx's to break up into conditions again
        
        % compute metrics (prob spike, independent prob)
        
        % store
    
    end
    
end